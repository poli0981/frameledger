// CsWin32 — the only P/Invoke surface besides NativeAntiCheatGuard (CLAUDE.md: native reachable
// only through Infrastructure). Marshaling is OFF (NativeMethods.json): DXGI objects are raw
// vtables, not RCWs, so no apartment ever gets a say in which thread may touch them.
CreateDXGIFactory1
IDXGIFactory6
IDXGIAdapter3
IDXGIDevice
DXGI_ADAPTER_DESC1
DXGI_ADAPTER_FLAG
DXGI_GPU_PREFERENCE
PdhOpenQueryW
PdhAddEnglishCounterW
PdhCollectQueryData
PdhGetFormattedCounterValue
PdhCloseQuery
PDH_FMT_COUNTERVALUE
PDH_FMT
QueryPerformanceCounter
QueryPerformanceFrequency
// The command pipe (07_IPC §C, P3 PR-1): created through CreateNamedPipe rather than the framework's
// constructor so the DACL is the SDDL PipeAccessControl states and PIPE_REJECT_REMOTE_CLIENTS is set
// explicitly — what the kernel enforces is what the code says, not a library default nobody can read.
CreateNamedPipe
SECURITY_ATTRIBUTES
FILE_FLAGS_AND_ATTRIBUTES
NAMED_PIPE_MODE
// The primary display's mode for hardware_snapshots.display_res / display_hz (P3 PR-3, Recording.PrimaryDisplay).
EnumDisplaySettings
DEVMODEW
ENUM_DISPLAY_SETTINGS_MODE
// The crash path's minidump (10_LOGGING §Crash handling, P4 PR-9, Diagnostics.CrashDumpWriter): the process's own
// memory into a file under crashdumps\, never another process's.
MiniDumpWriteDump
MINIDUMP_TYPE
// The machine beside the GPU (Telemetry.SystemTelemetrySource, 2026-09-21): CPU busy time and physical memory in use,
// two system-wide counters that open no process.
GetSystemTimes
GlobalMemoryStatusEx
MEMORYSTATUSEX
